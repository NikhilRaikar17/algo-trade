{
  "permissions": {
    "allow": [
      "Bash(curl --version)",
      "Bash(python -c \"import subprocess; r = subprocess.run\\([''which'', ''curl''], capture_output=True, text=True\\); print\\(r.stdout\\); r2 = subprocess.run\\([''where'', ''curl''], capture_output=True, text=True\\); print\\(r2.stdout\\)\")"
    ]
  }
}
